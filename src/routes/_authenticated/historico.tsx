import { createFileRoute, Link } from "@tanstack/react-router";
import { AppShell } from "@/components/app-shell";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { useJobs, useResumes } from "@/hooks/use-matchcv";
export const Route = createFileRoute("/_authenticated/historico")({ head: () => ({ meta: [{ title: "Histórico | MatchCV" }, { name: "description", content: "Acompanhe suas atividades no MatchCV." }, { property: "og:title", content: "Histórico | MatchCV" }, { property: "og:description", content: "Acompanhe suas atividades no MatchCV." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: HistoryPage });
function HistoryPage() { const { data: jobs = [] } = useJobs(); const { data: resumes = [] } = useResumes(); const events = [...jobs.map((item) => ({ id: item.id, date: item.created_at, title: `Vaga analisada: ${item.title}`, type: "Análise", to: `/vagas/${item.id}` })), ...resumes.map((item) => ({ id: item.id, date: item.created_at, title: `Currículo salvo: ${item.title}`, type: "Currículo", to: `/curriculos/${item.id}` }))].sort((a,b) => b.date.localeCompare(a.date)); return <AppShell title="Histórico" description="Sua jornada no MatchCV"><div className="space-y-3">{events.map((event) => <a key={`${event.type}-${event.id}`} href={event.to}><Card className="hover:border-brand"><CardContent className="flex items-center justify-between py-4"><div><p className="font-medium">{event.title}</p><p className="text-sm text-muted-foreground">{new Date(event.date).toLocaleString("pt-BR")}</p></div><Badge variant="outline">{event.type}</Badge></CardContent></Card></a>)}</div></AppShell>; }